{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sdd/schemas/global.schema.json",
  "title": "SDD Global State (sdd/global.json)",
  "description": "Central project state: modules and monorepo layout. Written by sdd-orchestrator (cycle start) and sdd-reviewer (cycle close). Cycle numbering is per-spec (cycles/cycle-XX), there is no global cycle counter.",
  "type": "object",
  "required": [
    "project",
    "description",
    "version",
    "completed_modules",
    "in_progress_modules",
    "pending_modules",
    "monorepo"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "project": { "type": "string", "minLength": 1 },
    "description": { "type": "string" },
    "version": { "type": "string" },
    "completed_modules": {
      "type": "array",
      "items": {
        "allOf": [
          { "$ref": "#/definitions/ModuleEntry" },
          { "required": ["cycles_completed", "completed_at", "description"] }
        ]
      }
    },
    "in_progress_modules": {
      "type": "array",
      "items": { "$ref": "#/definitions/ModuleEntry" }
    },
    "pending_modules": {
      "type": "array",
      "items": { "$ref": "#/definitions/ModuleEntry" }
    },
    "monorepo": {
      "type": "object",
      "required": ["tool", "package_manager", "apps", "libs"],
      "additionalProperties": false,
      "properties": {
        "tool": { "type": "string" },
        "package_manager": { "type": "string" },
        "apps": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "libs": {
          "description": "Name -> description map, same shape as `apps`, so the docs viewer can discover each lib's context. A plain string is still accepted for kits installed before this shape existed, but in that form libs are not listed in sdd:docs.",
          "oneOf": [
            { "type": "object", "additionalProperties": { "type": "string" } },
            { "type": "string" }
          ]
        }
      }
    }
  },
  "definitions": {
    "ModuleEntry": {
      "type": "object",
      "required": ["module", "spec", "apps"],
      "additionalProperties": false,
      "properties": {
        "module": { "type": "string", "minLength": 1 },
        "spec": {
          "type": "string",
          "pattern": "^spec-[a-z0-9-]+-\\d{3}-[a-z0-9-]+$"
        },
        "apps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^(apps|libs|tools)/[a-z][a-z0-9-]*$"
          }
        },
        "cycles_completed": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of cycles of THIS spec completed (per-spec numbering)."
        },
        "completed_at": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
        },
        "description": { "type": "string" }
      }
    }
  }
}
