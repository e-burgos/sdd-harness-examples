# Generated by @e-burgos/sdd-harness

# App ports (harness.config.json apps[].port — the code falls back to these defaults)
PORTAL_PORT=4200
ORDERS_API_PORT=8080

# PostgreSQL
DB_USER=admin
DB_PASSWORD=admin
DB_NAME=app_dev
DATABASE_URL=postgresql://admin:admin@localhost:5432/app_dev

# App
NODE_ENV=development
JWT_SECRET=change-me-in-production
