{
  "$schema": "./schemas/tools.schema.json",
  "_description": "Herramientas auxiliares del kit. rtk comprime la salida de los comandos de shell que leen los agentes (Claude Code y Gemini CLI) y viene activo por defecto; para apagarlo: `pnpm sdd:rtk -- --disable` (o pedírselo al sdd-steward). Este archivo es del proyecto: `harness update sdd` no lo toca.",
  "rtk": {
    "enabled": true,
    "auto_install": true
  }
}
